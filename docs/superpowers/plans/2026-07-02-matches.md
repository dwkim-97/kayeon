# 매칭(주선) 현황 관리 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 매물 간 주선(매칭) 현황을 상세 모달에서 관리한다 — 진행중/종료 상태, 메모, 카드 배지, 히스토리 기록.

**Architecture:** 신규 `matches` 테이블(여↔남 1:1 행, 1대다 허용). 기존 `/api/profiles` 패턴을 따른 REST 라우트. 상세 모달을 매칭 허브로 확장하고, Dashboard가 매칭 데이터를 로드해 카드 배지와 모달에 공급.

**Tech Stack:** Next.js 16 App Router, Supabase(Postgres+RLS), TypeScript, Vitest.

## Global Constraints

- Node arm64 실행: 모든 명령 앞에 `export PATH="$HOME/.local/node-arm64/bin:$PATH"`.
- dev/테스트 명령은 `pnpm` 사용. dev 서버는 `PORT=3100`.
- API 라우트는 `export const runtime = 'nodejs'`.
- 원격 push 금지 — 로컬 커밋만. push는 사용자 명시 지시 시에만.
- 상태 값: `'ongoing' | 'ended'`. 성별: 항상 female_id/male_id 명시.
- 커밋 메시지 끝에 `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.

---

## File Structure

- Create: `supabase/migrations/<ts>_matches.sql` — matches 테이블 + RLS + history 타입 확장
- Create: `src/types/match.ts` — `Match` 타입
- Modify: `src/types/history.ts` — `match_created`/`match_ended` 타입 추가
- Modify: `src/lib/supabase/types.ts` — matches Row/Insert/Update, history CHECK 반영
- Modify: `src/lib/supabase/mappers.ts` — `rowToMatch`
- Create: `src/lib/matches/summary.ts` (+test) — 진행중 카운트, 이성 후보 필터 순수함수
- Modify: `src/lib/history/events.ts` — match 라벨/설명
- Create: `src/app/api/matches/route.ts` — GET(list) / POST(create)
- Create: `src/app/api/matches/[id]/route.ts` — PATCH / DELETE
- Modify: `src/components/ProfileDetailModal.tsx` — 매칭 허브 UI
- Modify: `src/components/ProfileCard.tsx` (+test) — 매칭 배지
- Modify: `src/components/Dashboard.tsx` — 매칭 로드/연동

---

### Task 1: DB 마이그레이션 + prod 적용

**Files:**
- Create: `supabase/migrations/20260702_matches.sql`

**Interfaces:**
- Produces: `public.matches` 테이블(id, female_id, male_id, status, memo, created_by_name, created_at, ended_at), history_events CHECK에 match_created/match_ended.

- [ ] **Step 1: 마이그레이션 파일 작성**

`supabase/migrations/20260702000000_matches.sql`:

```sql
create table public.matches (
  id uuid primary key default gen_random_uuid(),
  female_id uuid not null references public.profiles (id) on delete cascade,
  male_id uuid not null references public.profiles (id) on delete cascade,
  status text not null default 'ongoing',
  memo text not null default '',
  created_by_name text not null,
  created_at timestamptz not null default now(),
  ended_at timestamptz,
  constraint matches_status_check check (status in ('ongoing', 'ended')),
  constraint matches_created_by_name_not_blank check (btrim(created_by_name) <> '')
);

create index matches_female_id_idx on public.matches (female_id);
create index matches_male_id_idx on public.matches (male_id);
create unique index matches_unique_ongoing on public.matches (female_id, male_id) where status = 'ongoing';

alter table public.matches enable row level security;

create policy matches_select_app_users on public.matches
  for select to authenticated using ((select public.is_app_user()));
create policy matches_insert_app_users on public.matches
  for insert to authenticated with check ((select public.is_app_user()));
create policy matches_update_app_users on public.matches
  for update to authenticated using ((select public.is_app_user())) with check ((select public.is_app_user()));
create policy matches_delete_app_users on public.matches
  for delete to authenticated using ((select public.is_app_user()));

grant select, insert, update, delete on public.matches to authenticated;

alter table public.history_events drop constraint history_events_type_check;
alter table public.history_events add constraint history_events_type_check check (
  type in (
    'profile_created','profile_updated','profile_deleted',
    'profile_blocked','profile_activated','admin_created','admin_removed',
    'match_created','match_ended'
  )
);
```

- [ ] **Step 2: prod DB에 직접 적용** (사용자가 "DB 바로 적용" 승인함)

secret key로 마이그레이션 SQL을 실제 DB에 실행하는 노드 스크립트로 적용. Supabase는 REST로 임의 DDL을 못 돌리므로 `pg`가 없으면 Supabase SQL 실행용으로 `postgres` 연결이 필요 → **실제 적용은 사용자 Supabase 대시보드 SQL 에디터 또는 psql**. 대안으로, 적용 가능 여부를 먼저 점검한다:

Run:
```bash
export PATH="$HOME/.local/node-arm64/bin:$PATH"
node --env-file=.env.local -e '
const url=process.env.NEXT_PUBLIC_SUPABASE_URL, secret=process.env.SUPABASE_SECRET_KEY;
(async()=>{
  const r=await fetch(`${url}/rest/v1/matches?select=id&limit=1`,{headers:{apikey:secret,Authorization:`Bearer ${secret}`}});
  console.log("matches 테이블 존재 여부:", r.status===200 ? "있음" : `없음(${r.status})`);
})();
'
```
Expected: 적용 전 "없음(404)" → 적용 후 "있음". (적용 방법은 Step 3에서 확정)

- [ ] **Step 3: 적용 실행 확인**

DDL 실행 후 위 스크립트를 재실행해 `matches 테이블 존재: 있음` 확인.
Expected: `있음`

- [ ] **Step 4: Commit**

```bash
git add supabase/migrations/20260702000000_matches.sql
git commit -m "feat: add matches table migration (주선 현황)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: 타입 정의 (Match, history, Database)

**Files:**
- Create: `src/types/match.ts`
- Modify: `src/types/history.ts`
- Modify: `src/lib/supabase/types.ts:110` (history_events Row.type), matches Tables 추가

**Interfaces:**
- Produces: `Match` 타입 { id, femaleId, maleId, status, memo, createdByName, createdAt, endedAt }; `MatchStatus = 'ongoing'|'ended'`; history 타입에 match_created/match_ended.

- [ ] **Step 1: Match 타입 작성**

`src/types/match.ts`:
```typescript
export type MatchStatus = 'ongoing' | 'ended';

export type Match = {
  id: string;
  femaleId: string;
  maleId: string;
  status: MatchStatus;
  memo: string;
  createdByName: string;
  createdAt: string;
  endedAt: string | null;
};
```

- [ ] **Step 2: history 타입 확장**

`src/types/history.ts`의 `HistoryEventType` 유니온에 추가:
```typescript
export type HistoryEventType = ProfileEventType | 'admin_created' | 'admin_removed' | 'match_created' | 'match_ended';
```

- [ ] **Step 3: Database 타입에 matches 추가**

`src/lib/supabase/types.ts`의 `Tables`에 matches Row/Insert/Update 추가:
```typescript
matches: {
  Row: {
    id: string;
    female_id: string;
    male_id: string;
    status: 'ongoing' | 'ended';
    memo: string;
    created_by_name: string;
    created_at: string;
    ended_at: string | null;
  };
  Insert: {
    id?: string;
    female_id: string;
    male_id: string;
    status?: 'ongoing' | 'ended';
    memo?: string;
    created_by_name: string;
    created_at?: string;
    ended_at?: string | null;
  };
  Update: {
    status?: 'ongoing' | 'ended';
    memo?: string;
    ended_at?: string | null;
  };
  Relationships: [];
};
```

- [ ] **Step 4: 타입체크**

Run: `export PATH="$HOME/.local/node-arm64/bin:$PATH" && pnpm exec tsc --noEmit`
Expected: 에러 없음

- [ ] **Step 5: Commit**

```bash
git add src/types/match.ts src/types/history.ts src/lib/supabase/types.ts
git commit -m "feat: add Match types and history match events

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: mapper (rowToMatch)

**Files:**
- Modify: `src/lib/supabase/mappers.ts`

**Interfaces:**
- Consumes: `Match` (Task 2), `Database['public']['Tables']['matches']['Row']`.
- Produces: `rowToMatch(row): Match`.

- [ ] **Step 1: rowToMatch 추가**

`src/lib/supabase/mappers.ts` 상단 타입 import에 Match 추가하고 함수 작성:
```typescript
import type {Match} from '@/types/match';

type MatchRow = Database['public']['Tables']['matches']['Row'];

export function rowToMatch(row: MatchRow): Match {
  return {
    id: row.id,
    femaleId: row.female_id,
    maleId: row.male_id,
    status: row.status,
    memo: row.memo,
    createdByName: row.created_by_name,
    createdAt: row.created_at,
    endedAt: row.ended_at,
  };
}
```

- [ ] **Step 2: 타입체크**

Run: `export PATH="$HOME/.local/node-arm64/bin:$PATH" && pnpm exec tsc --noEmit`
Expected: 에러 없음

- [ ] **Step 3: Commit**

```bash
git add src/lib/supabase/mappers.ts
git commit -m "feat: add rowToMatch mapper

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: 순수 로직 (진행중 카운트 + 이성 후보 필터)

**Files:**
- Create: `src/lib/matches/summary.ts`
- Test: `src/lib/matches/summary.test.ts`

**Interfaces:**
- Consumes: `Match` (Task 2), `Profile`, `Gender`.
- Produces:
  - `countOngoingByProfile(matches: Match[]): Map<string, number>` — 프로필 id별 진행중 매칭 수(female+male 양쪽 카운트).
  - `getMatchCandidates(profile: Profile, all: Profile[]): Profile[]` — 반대 성별 + active 매물.
  - `getProfileMatches(profileId: string, matches: Match[]): Match[]` — 해당 프로필이 속한 매칭.

- [ ] **Step 1: 실패 테스트 작성**

`src/lib/matches/summary.test.ts`:
```typescript
import {describe, expect, it} from 'vitest';
import {countOngoingByProfile, getMatchCandidates, getProfileMatches} from './summary';
import type {Match} from '@/types/match';
import type {Profile} from '@/types/profile';

const match = (over: Partial<Match>): Match => ({
  id: 'm', femaleId: 'f', maleId: 'm1', status: 'ongoing', memo: '',
  createdByName: 'A', createdAt: '2026-07-01T00:00:00Z', endedAt: null, ...over,
});
const profile = (over: Partial<Profile>): Profile => ({
  id: 'p', gender: 'female', status: 'active', isActivated: true, authorName: 'A',
  starredByName: null, residence: 'x', birthYear: 1996, height: 160, job: 'j',
  religion: 'not_selected', mbti: '', hobbies: '', smoking: 'not_selected',
  drinking: 'not_selected', idealType: '', matchmakerComment: '', extra: '',
  photos: [], createdAt: '2026-07-01T00:00:00Z', updatedAt: '2026-07-01T00:00:00Z', ...over,
});

describe('countOngoingByProfile', () => {
  it('counts ongoing matches for both female and male sides', () => {
    const m = countOngoingByProfile([
      match({id: 'm1', femaleId: 'f1', maleId: 'g1'}),
      match({id: 'm2', femaleId: 'f1', maleId: 'g2'}),
      match({id: 'm3', femaleId: 'f2', maleId: 'g1', status: 'ended'}),
    ]);
    expect(m.get('f1')).toBe(2);
    expect(m.get('g1')).toBe(1);   // ended 제외
    expect(m.get('g2')).toBe(1);
    expect(m.get('f2')).toBeUndefined();
  });
});

describe('getMatchCandidates', () => {
  it('returns opposite-gender active profiles', () => {
    const female = profile({id: 'f1', gender: 'female'});
    const maleActive = profile({id: 'g1', gender: 'male'});
    const maleBlocked = profile({id: 'g2', gender: 'male', isActivated: false, status: 'blocked'});
    const otherFemale = profile({id: 'f2', gender: 'female'});
    const result = getMatchCandidates(female, [female, maleActive, maleBlocked, otherFemale]);
    expect(result.map(p => p.id)).toEqual(['g1']);
  });
});

describe('getProfileMatches', () => {
  it('returns matches where profile is female or male side', () => {
    const matches = [match({id: 'm1', femaleId: 'f1'}), match({id: 'm2', maleId: 'f1'}), match({id: 'm3', femaleId: 'x', maleId: 'y'})];
    expect(getProfileMatches('f1', matches).map(m => m.id)).toEqual(['m1', 'm2']);
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `export PATH="$HOME/.local/node-arm64/bin:$PATH" && pnpm test src/lib/matches/summary.test.ts`
Expected: FAIL (모듈 없음)

- [ ] **Step 3: 구현**

`src/lib/matches/summary.ts`:
```typescript
import type {Match} from '@/types/match';
import type {Profile} from '@/types/profile';

export function countOngoingByProfile(matches: Match[]): Map<string, number> {
  const counts = new Map<string, number>();
  for (const m of matches) {
    if (m.status !== 'ongoing') continue;
    counts.set(m.femaleId, (counts.get(m.femaleId) ?? 0) + 1);
    counts.set(m.maleId, (counts.get(m.maleId) ?? 0) + 1);
  }
  return counts;
}

export function getMatchCandidates(profile: Profile, all: Profile[]): Profile[] {
  const opposite = profile.gender === 'female' ? 'male' : 'female';
  return all.filter(p => p.gender === opposite && p.isActivated);
}

export function getProfileMatches(profileId: string, matches: Match[]): Match[] {
  return matches.filter(m => m.femaleId === profileId || m.maleId === profileId);
}
```

- [ ] **Step 4: 통과 확인**

Run: `export PATH="$HOME/.local/node-arm64/bin:$PATH" && pnpm test src/lib/matches/summary.test.ts`
Expected: PASS (3 tests)

- [ ] **Step 5: Commit**

```bash
git add src/lib/matches/summary.ts src/lib/matches/summary.test.ts
git commit -m "feat: add match summary pure helpers

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: history 이벤트 라벨/설명

**Files:**
- Modify: `src/lib/history/events.ts`

**Interfaces:**
- Consumes: `historyEventLabels` record (기존).
- Produces: `historyEventLabels`에 match_created/match_ended 라벨.

- [ ] **Step 1: 라벨 추가**

`src/lib/history/events.ts`의 `historyEventLabels`에 추가:
```typescript
  match_created: '매칭 연결',
  match_ended: '매칭 종료',
```
(record 타입이 HistoryEventType 전체를 요구하므로 이 두 줄 추가로 타입 완성됨)

- [ ] **Step 2: 타입체크**

Run: `export PATH="$HOME/.local/node-arm64/bin:$PATH" && pnpm exec tsc --noEmit`
Expected: 에러 없음

- [ ] **Step 3: Commit**

```bash
git add src/lib/history/events.ts
git commit -m "feat: add match history event labels

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: API — GET/POST /api/matches

**Files:**
- Create: `src/app/api/matches/route.ts`

**Interfaces:**
- Consumes: `createSupabaseServerClient`, `getSessionUserName`, `rowToMatch`.
- Produces: `GET /api/matches` → `{matches: Match[]}`; `POST /api/matches` body `{femaleId, maleId}` → `{match: Match}` (201) or `{message}` (4xx/5xx).

- [ ] **Step 1: 라우트 작성**

`src/app/api/matches/route.ts`:
```typescript
import {NextResponse} from 'next/server';

import {getSessionUserName} from '@/lib/auth/session';
import {rowToMatch} from '@/lib/supabase/mappers';
import {createSupabaseServerClient} from '@/lib/supabase/server';

export const runtime = 'nodejs';

export async function GET(request: Request) {
  const supabase = await createSupabaseServerClient();
  const profileId = new URL(request.url).searchParams.get('profileId');

  let query = supabase.from('matches').select('*').order('created_at', {ascending: false});
  if (profileId) query = query.or(`female_id.eq.${profileId},male_id.eq.${profileId}`);

  const {data, error} = await query;
  if (error) return NextResponse.json({message: error.message}, {status: 500});
  return NextResponse.json({matches: data.map(rowToMatch)});
}

export async function POST(request: Request) {
  const supabase = await createSupabaseServerClient();
  const actorName = await getSessionUserName(supabase);
  if (!actorName) return NextResponse.json({message: '인증이 필요합니다.'}, {status: 401});

  const {femaleId, maleId} = (await request.json()) as {femaleId: string; maleId: string};
  if (!femaleId || !maleId) return NextResponse.json({message: '여성/남성 매물을 모두 선택해 주세요.'}, {status: 400});

  const {data, error} = await supabase
    .from('matches')
    .insert({female_id: femaleId, male_id: maleId, created_by_name: actorName})
    .select('*')
    .single();

  if (error) {
    const isDup = error.code === '23505';
    return NextResponse.json({message: isDup ? '이미 진행중인 매칭입니다.' : error.message}, {status: isDup ? 409 : 500});
  }
  return NextResponse.json({match: rowToMatch(data)}, {status: 201});
}
```

- [ ] **Step 2: 수동 검증 (dev 서버)**

Run: dev 서버 확인 후 (로그인 세션 필요 — 브라우저에서 실제 확인). 최소 컴파일 확인:
`export PATH="$HOME/.local/node-arm64/bin:$PATH" && curl -s -m 15 -o /dev/null -w "%{http_code}\n" http://localhost:3100/api/matches`
Expected: 200 (빈 배열) 또는 인증 리다이렉트 없음(공개 아님이면 미들웨어 통과 후 200)

- [ ] **Step 3: Commit**

```bash
git add src/app/api/matches/route.ts
git commit -m "feat: add GET/POST /api/matches

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 7: API — PATCH/DELETE /api/matches/[id]

**Files:**
- Create: `src/app/api/matches/[id]/route.ts`

**Interfaces:**
- Produces: `PATCH /api/matches/[id]` body `{status?, memo?}` → `{match}`; `DELETE /api/matches/[id]` → `{ok:true}`.

- [ ] **Step 1: 라우트 작성**

`src/app/api/matches/[id]/route.ts`:
```typescript
import {NextResponse} from 'next/server';

import {rowToMatch} from '@/lib/supabase/mappers';
import {createSupabaseServerClient} from '@/lib/supabase/server';
import type {MatchStatus} from '@/types/match';

export const runtime = 'nodejs';

type RouteParams = {params: Promise<{id: string}>};

export async function PATCH(request: Request, {params}: RouteParams) {
  const {id} = await params;
  const supabase = await createSupabaseServerClient();
  const body = (await request.json()) as {status?: MatchStatus; memo?: string};

  const update: {status?: MatchStatus; memo?: string; ended_at?: string | null} = {};
  if (body.status !== undefined) {
    update.status = body.status;
    update.ended_at = body.status === 'ended' ? new Date().toISOString() : null;
  }
  if (body.memo !== undefined) update.memo = body.memo;

  const {data, error} = await supabase.from('matches').update(update).eq('id', id).select('*').single();
  if (error) return NextResponse.json({message: error.message}, {status: 500});
  return NextResponse.json({match: rowToMatch(data)});
}

export async function DELETE(_request: Request, {params}: RouteParams) {
  const {id} = await params;
  const supabase = await createSupabaseServerClient();
  const {error} = await supabase.from('matches').delete().eq('id', id);
  if (error) return NextResponse.json({message: error.message}, {status: 500});
  return NextResponse.json({ok: true});
}
```

- [ ] **Step 2: 타입체크**

Run: `export PATH="$HOME/.local/node-arm64/bin:$PATH" && pnpm exec tsc --noEmit`
Expected: 에러 없음

- [ ] **Step 3: Commit**

```bash
git add "src/app/api/matches/[id]/route.ts"
git commit -m "feat: add PATCH/DELETE /api/matches/[id]

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 8: 카드 매칭 배지 (ProfileCard)

**Files:**
- Modify: `src/components/ProfileCard.tsx`
- Test: `src/components/ProfileCard.test.tsx`

**Interfaces:**
- Consumes: 기존 ProfileCardProps.
- Produces: `ProfileCard`에 `ongoingMatchCount?: number` prop. >0이면 배지 렌더.

- [ ] **Step 1: 실패 테스트 추가**

`src/components/ProfileCard.test.tsx`에 추가(기존 describe 안):
```typescript
  it('shows a match badge when there are ongoing matches', () => {
    render(<ProfileCard {...defaultProps} ongoingMatchCount={2} />);
    expect(screen.getByText(/매칭 2/)).toBeInTheDocument();
  });

  it('shows no match badge when count is zero', () => {
    render(<ProfileCard {...defaultProps} ongoingMatchCount={0} />);
    expect(screen.queryByText(/매칭/)).not.toBeInTheDocument();
  });
```

- [ ] **Step 2: 실패 확인**

Run: `export PATH="$HOME/.local/node-arm64/bin:$PATH" && pnpm test src/components/ProfileCard.test.tsx`
Expected: FAIL (배지 없음)

- [ ] **Step 3: 구현**

`ProfileCardProps`에 `ongoingMatchCount?: number;` 추가, 구조분해에 `ongoingMatchCount = 0,` 추가.
사진 영역(등록자 뱃지 근처, 우측 상단)에 배지 추가 — `{profile.starredByName}` 등록자 뱃지 블록 아래에:
```tsx
          {ongoingMatchCount > 0 ? (
            <div className={`absolute right-3 top-3 z-20 rounded-full bg-pink-500/90 font-black text-white shadow-sm ${
              isCompact ? 'px-2 py-0.5 text-[10px]' : 'px-2.5 py-1 text-xs'
            }`}>
              💞 매칭 {ongoingMatchCount}
            </div>
          ) : null}
```

- [ ] **Step 4: 통과 확인**

Run: `export PATH="$HOME/.local/node-arm64/bin:$PATH" && pnpm test src/components/ProfileCard.test.tsx`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/components/ProfileCard.tsx src/components/ProfileCard.test.tsx
git commit -m "feat: add ongoing match badge to ProfileCard

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 9: 상세 모달 매칭 허브 (ProfileDetailModal)

**Files:**
- Modify: `src/components/ProfileDetailModal.tsx`

**Interfaces:**
- Consumes: `Match`, `Profile`, `getProfileMatches`/`getMatchCandidates` (Task 4).
- Produces: `ProfileDetailModal` props에 `matches: Match[]`, `allProfiles: Profile[]`, `onCreateMatch(femaleId, maleId)`, `onEndMatch(matchId)`, `onDeleteMatch(matchId)`, `onOpenProfile(profileId)`.

- [ ] **Step 1: props 확장 + 매칭 섹션 구현**

`ProfileDetailModalProps`에 위 props 추가. 정보 패널(`<ul>` 아래, overflow 영역 안)에 매칭 섹션 추가:
```tsx
        {/* 매칭 현황 */}
        <div className="border-t border-[var(--border)] px-5 py-4">
          <h3 className="mb-2 text-sm font-black text-[var(--violet-900)]">매칭 현황</h3>
          {profileMatches.length === 0 ? (
            <p className="text-sm text-slate-400">아직 연결된 매칭이 없습니다.</p>
          ) : (
            <ul className="space-y-2">
              {profileMatches.map(m => {
                const partnerId = m.femaleId === profile.id ? m.maleId : m.femaleId;
                const partner = allProfiles.find(p => p.id === partnerId);
                return (
                  <li key={m.id} className="flex items-center gap-2 rounded-[8px] border border-[var(--violet-100)] px-3 py-2 text-sm">
                    <button className="min-w-0 flex-1 truncate text-left font-bold text-[var(--violet-900)] hover:underline"
                      type="button" onClick={() => partner && onOpenProfile(partner.id)}>
                      {partner ? `${formatBirthYearLabel(partner.birthYear)} · ${partner.residence}` : '(삭제된 매물)'}
                    </button>
                    <span className={`shrink-0 rounded-full px-2 py-0.5 text-xs font-bold ${
                      m.status === 'ongoing' ? 'bg-pink-100 text-pink-700' : 'bg-slate-100 text-slate-500'
                    }`}>{m.status === 'ongoing' ? '진행중' : '종료'}</span>
                    {m.status === 'ongoing' ? (
                      <button className="shrink-0 rounded-[6px] border border-slate-200 px-2 py-1 text-xs font-bold text-slate-600 hover:bg-slate-50"
                        type="button" onClick={() => onEndMatch(m.id)}>종료</button>
                    ) : null}
                    <button className="shrink-0 rounded-[6px] border border-red-100 px-2 py-1 text-xs font-bold text-[var(--danger)] hover:bg-red-50"
                      type="button" onClick={() => onDeleteMatch(m.id)}>삭제</button>
                  </li>
                );
              })}
            </ul>
          )}

          {showCandidates ? (
            <div className="mt-3 max-h-52 space-y-1 overflow-y-auto rounded-[8px] border border-[var(--violet-100)] p-2">
              {candidates.length === 0 ? (
                <p className="p-2 text-sm text-slate-400">연결 가능한 이성 매물이 없습니다.</p>
              ) : candidates.map(c => (
                <button key={c.id} type="button"
                  className="block w-full truncate rounded-[6px] px-2 py-1.5 text-left text-sm hover:bg-[var(--violet-50)]"
                  onClick={() => handleCreate(c)}>
                  {formatBirthYearLabel(c.birthYear)} · {c.residence} · {c.job}
                </button>
              ))}
            </div>
          ) : (
            <button className="mt-3 inline-flex h-9 items-center gap-1.5 rounded-[8px] bg-[var(--violet-600)] px-3 text-sm font-bold text-white hover:bg-[var(--violet-700)]"
              type="button" onClick={() => setShowCandidates(true)}>+ 매칭 추가</button>
          )}
        </div>
```

컴포넌트 상단에 상태/파생값:
```tsx
  const [showCandidates, setShowCandidates] = useState(false);
  const profileMatches = getProfileMatches(profile.id, matches);
  const candidates = getMatchCandidates(profile, allProfiles);

  const handleCreate = (partner: Profile) => {
    const femaleId = profile.gender === 'female' ? profile.id : partner.id;
    const maleId = profile.gender === 'female' ? partner.id : profile.id;
    onCreateMatch(femaleId, maleId);
    setShowCandidates(false);
  };
```
import 추가: `useState`(이미 없으면), `getProfileMatches`, `getMatchCandidates`, `formatBirthYearLabel`(이미 있음).

- [ ] **Step 2: 타입체크**

Run: `export PATH="$HOME/.local/node-arm64/bin:$PATH" && pnpm exec tsc --noEmit`
Expected: 에러 없음 (Dashboard가 아직 새 props 안 넘기면 에러 → Task 10에서 해결. 이 태스크는 컴포넌트 단독 타입만 확인하고 넘어가되, 필요한 props는 required로 두어 Task 10에서 연결)

- [ ] **Step 3: Commit**

```bash
git add src/components/ProfileDetailModal.tsx
git commit -m "feat: match hub UI in ProfileDetailModal

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 10: Dashboard 연동

**Files:**
- Modify: `src/components/Dashboard.tsx`

**Interfaces:**
- Consumes: Task 4 helpers, Task 6/7 API, Task 8/9 컴포넌트 props.
- Produces: 매칭 state 로드/생성/종료/삭제, 카드 배지 카운트 전달, 모달에 매칭 props 전달.

- [ ] **Step 1: 매칭 state + 로드**

import 추가: `import type {Match} from '@/types/match';`, `import {countOngoingByProfile} from '@/lib/matches/summary';`.
state: `const [matches, setMatches] = useState<Match[]>([]);`
로드 effect(프로필 로드 effect 옆):
```tsx
  useEffect(() => {
    fetch('/api/matches').then(r => r.json()).then(({matches: m}) => setMatches(m ?? [])).catch(() => setMatches([]));
  }, []);
  const ongoingCounts = useMemo(() => countOngoingByProfile(matches), [matches]);
```

- [ ] **Step 2: 핸들러 추가**

```tsx
  const handleCreateMatch = async (femaleId: string, maleId: string) => {
    const res = await fetch('/api/matches', {method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify({femaleId, maleId})});
    if (!res.ok) { const {message} = await res.json(); setAlertState({kind: 'alert', title: '매칭 실패', message}); return; }
    const {match} = await res.json();
    setMatches(cur => [match, ...cur]);
    const f = profiles.find(p => p.id === femaleId), m = profiles.find(p => p.id === maleId);
    if (f && m) recordHistory({type: 'match_created', actorName: authorName,
      targetLabel: `${formatBirthYearLabel(f.birthYear)} ↔ ${formatBirthYearLabel(m.birthYear)}`,
      description: '매칭을 연결했습니다.'});
  };

  const handleEndMatch = async (matchId: string) => {
    const res = await fetch(`/api/matches/${matchId}`, {method: 'PATCH', headers: {'Content-Type': 'application/json'}, body: JSON.stringify({status: 'ended'})});
    if (!res.ok) return;
    const {match} = await res.json();
    setMatches(cur => cur.map(x => x.id === matchId ? match : x));
    recordHistory({type: 'match_ended', actorName: authorName, targetLabel: '매칭', description: '매칭을 종료했습니다.'});
  };

  const handleDeleteMatch = async (matchId: string) => {
    const res = await fetch(`/api/matches/${matchId}`, {method: 'DELETE'});
    if (!res.ok) return;
    setMatches(cur => cur.filter(x => x.id !== matchId));
  };
```

- [ ] **Step 3: ProfileCard에 카운트 전달**

ProfileCard 사용처에 추가: `ongoingMatchCount={ongoingCounts.get(profile.id) ?? 0}`

- [ ] **Step 4: 상세 모달 상태 + props 연결**

ProfileCard가 자체적으로 모달을 여는 현재 구조에서, 매칭 props를 넘기려면 Dashboard가 모달을 제어해야 함. ProfileCard의 `isDetailOpen` 내부 모달을 Dashboard 레벨로 승격:
- ProfileCard: `onOpenDetail(profile)` prop 추가, 사진 클릭 시 호출. 내부 `ProfileDetailModal` 렌더 제거.
- Dashboard: `const [detailProfile, setDetailProfile] = useState<Profile | null>(null);`, 사진 클릭 시 setDetailProfile. 모달 렌더:
```tsx
      {detailProfile ? (
        <ProfileDetailModal
          profile={detailProfile}
          matches={matches}
          allProfiles={profiles}
          onCreateMatch={handleCreateMatch}
          onEndMatch={handleEndMatch}
          onDeleteMatch={handleDeleteMatch}
          onOpenProfile={(pid) => setDetailProfile(profiles.find(p => p.id === pid) ?? null)}
          onClose={() => setDetailProfile(null)}
        />
      ) : null}
```
import `ProfileDetailModal` into Dashboard.

- [ ] **Step 5: 전체 검증**

Run:
```bash
export PATH="$HOME/.local/node-arm64/bin:$PATH"
pnpm test && pnpm exec tsc --noEmit && pnpm lint
```
Expected: 테스트 all pass, 타입 OK, 린트 내 파일 error 0

- [ ] **Step 6: Commit**

```bash
git add src/components/Dashboard.tsx src/components/ProfileCard.tsx
git commit -m "feat: wire matches into Dashboard (badge, modal hub)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 11: 브라우저 검증

- [ ] **Step 1:** dev 서버에서 로그인 → 여성 매물 상세 열기 → "매칭 추가" → 남성 리스트 클릭 → 매칭 생성 확인.
- [ ] **Step 2:** 카드에 💞 배지, 상대 프로필의 매칭 현황에도 반영 확인.
- [ ] **Step 3:** 종료/삭제 동작, 히스토리 페이지에 match_created/ended 기록 확인.
- [ ] **Step 4:** 중복 매칭 시도 → "이미 진행중" 에러 확인.

---

## Self-Review 결과

- 스펙 커버리지: matches 테이블(T1) / 타입(T2,3) / 상태·메모(T2,6,7) / 카드 배지(T8) / 모달 허브(T9) / 히스토리(T5,10) / 이성 후보 여↔남(T4,9) — 전부 태스크 있음.
- 타입 일관성: `Match` 필드명(femaleId/maleId/status/memo/createdByName/createdAt/endedAt) T2 정의 → T3 mapper → T4 helper → T9/10 사용 일치.
- 플레이스홀더: 없음(모든 코드 블록 실체 포함).
- 주의: T1 DB 적용 방법(psql/대시보드 vs 스크립트)은 실행 시 확정 필요 — 아래 실행 핸드오프에서 다룸.
